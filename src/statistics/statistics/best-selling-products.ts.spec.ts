import { BestSellingProduct } from './best-selling-products.ts';

describe('BestSellingProductsTs', () => {
  it('should be defined', () => {
    expect(new BestSellingProduct()).toBeDefined();
  });
});
